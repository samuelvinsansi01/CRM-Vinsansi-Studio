CRM Vinsansi Studio R59 — banco congelado

As migrations históricas foram removidas deste pacote LIMPO porque descreviam camadas que já foram eliminadas do banco final.
Não reaplique migrations antigas sobre o banco atual.

Para conferir o contrato do banco, execute somente o arquivo de leitura:
  CHECK - CRM R59 - Homologacao final.sql

As Supabase Functions atuais permanecem neste diretório.
