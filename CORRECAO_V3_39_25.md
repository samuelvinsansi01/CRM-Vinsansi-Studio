# Correção V3.39.25

O filtro de encurtadores agora é aplicado em todos os lados da deduplicação e no `lead_registry`. Registros legados com `normalizedSite = bit.ly` ou `tinyurl.com` não voltam a ser usados como identidade de empresa.
