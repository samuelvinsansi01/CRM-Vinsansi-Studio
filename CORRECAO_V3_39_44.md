# V3.39.44

- Expõe o estado/UF dos leads no endpoint da extensão.
- Não altera telas, templates, importação, filas, Worker ou regras existentes.
- Suporta leitura de `state`, `uf` e `estado` nas colunas e objetos JSON do lead.
