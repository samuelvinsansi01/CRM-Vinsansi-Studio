import type { ApiRequest,ApiResponse } from '../../../server/maps/shared.js';
import { send,setCors } from '../../../server/maps/shared.js';
import { executorStatus,sessionScope } from '../../../server/tools/executor.js';
export default async function handler(req:ApiRequest,res:ApiResponse){setCors(req,res);if(req.method==='OPTIONS')return res.status(204).end();if(req.method!=='GET')return send(req,res,405,{error:'method_not_allowed'});try{const scope=await sessionScope(req);return send(req,res,200,{ok:true,toolId:scope.toolId,externalInstallationId:scope.externalInstallationId,organizationId:scope.organizationId,organizationName:scope.context.organizationName,memberId:scope.context.memberId,accessLevel:scope.context.accessLevel,permissions:scope.context.permissions,eligibleOrganizations:scope.eligibleOrganizations});}catch(error){return send(req,res,executorStatus(error),{ok:false,error:error instanceof Error?error.message:String(error)});}}
