import fs from 'node:fs';import path from 'node:path';import crypto from 'node:crypto';import {fileURLToPath} from 'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const skipDirs=new Set(['node_modules','.git','dist','.vercel','.cache']);const skipFiles=new Set(['R60_CANDIDATE_HASHES.txt']);const files=[];function walk(d){for(const e of fs.readdirSync(d,{withFileTypes:true})){if(e.isDirectory()&&skipDirs.has(e.name))continue;const p=path.join(d,e.name);if(e.isDirectory())walk(p);else if(e.isFile()){const rel=path.relative(root,p).replaceAll(path.sep,'/');if(skipFiles.has(rel)||/[.](zip|log|map)$/i.test(e.name))continue;files.push(p)}}}walk(root);const rows=files.map(p=>({p:path.relative(root,p).replaceAll(path.sep,'/'),h:crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex')})).sort((a,b)=>a.p<b.p?-1:a.p>b.p?1:0);console.log(crypto.createHash('sha256').update(rows.map(x=>`${x.p}\0${x.h}\n`).join('')).digest('hex'));
