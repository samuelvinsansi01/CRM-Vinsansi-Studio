import fs from 'node:fs';
import path from 'node:path';
const root=path.resolve(process.cwd());
const read=(p)=>fs.readFileSync(path.join(root,p),'utf8');
const expect=(condition,message)=>{if(!condition)throw new Error(message)};
const sql=read('supabase/migrations/20260825210500_stage5_manual_reply_remote_jid.sql');
const channel=read('server/routes/whatsapp/conversation-channel.ts');
expect(sql.includes("coalesce(nullif(trim(v_c.remote_jid),''),nullif(trim(v_c.contact_phone),'')"),'prepare manual must prefer remote_jid');
expect(channel.includes('text(found.data.remote_jid)||text(found.data.contact_phone)'),'channel must prefer remote_jid');
console.log('R28 resposta manual por JID canônico: PASS (2 verificações)');
