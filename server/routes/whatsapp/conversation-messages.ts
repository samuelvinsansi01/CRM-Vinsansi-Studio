import { failure,humanScope,integer,query,rpc,send,type Stage5Request,type Stage5Response } from '../../whatsapp/stage5.js';
export default async function handler(req:Stage5Request,res:Stage5Response){if(req.method!=='GET')return send(res,405,{ok:false,error:'method_not_allowed'});try{const scope=await humanScope(req,'whatsapp.view');const data=await rpc(scope,'service_stage5_list_messages',{p_conversations_id:integer(query(req,'conversationId'),'conversation_id_required'),p_before_id:integer(query(req,'beforeId'),'before_id_invalid',true),p_after_id:integer(query(req,'afterId'),'after_id_invalid',true),p_limit:Math.min(100,Math.max(1,Number(query(req,'limit')||50)))});return send(res,200,{ok:true,...(data as object)});}catch(error){return failure(res,error);}}
