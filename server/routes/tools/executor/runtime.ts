import type { ApiRequest,ApiResponse,Row } from '../../../maps/shared.js';
import { body,send,setCors,text } from '../../../maps/shared.js';
import { executorStatus,installationScope } from '../../../tools/executor.js';

const TABLES=new Set(['status','lead_status','channels','chips','instances','queue_items','leads','templates','branches','sents','worker_batches','service_worker_heartbeats']);
const GLOBAL_TABLES=new Set(['status','lead_status','channels']);
const RPCS=new Set(['service_get_evolution_instances','worker_claim_dispatch_part','worker_complete_dispatch_part','worker_finalize_whatsapp_queue_item','worker_fail_whatsapp_queue_item','worker_start_whatsapp_batch','worker_set_whatsapp_batch_state','worker_claim_next_batch_item','worker_complete_batch_item','refresh_operational_alerts','service_worker_heartbeat','service_claim_recovery_request','service_complete_recovery_request','worker_recover_stale_whatsapp_v2','instagram_recover_stale_items_v2','service_orchestrate_ready_leads','service_stage5_converge_automatic_message']);
const OPERATORS=new Set(['eq','neq','gt','gte','lt','lte','in','is','not.eq','not.is']);

function applyFilters(query:any,filters:unknown,organizationId:number,global:boolean){let next=query;if(!global)next=next.eq('organizations_id',organizationId);for(const raw of Array.isArray(filters)?filters:[]){const filter=raw as Row;const column=text(filter.column);const operator=text(filter.operator);if(!column||column==='users_id'||column==='organizations_id'||!OPERATORS.has(operator))continue;if(operator==='in')next=next.in(column,Array.isArray(filter.value)?filter.value:[]);else if(operator.startsWith('not.'))next=next.not(column,operator.slice(4),filter.value);else next=next[operator](column,filter.value);}return next;}
export default async function handler(req:ApiRequest,res:ApiResponse){setCors(req,res);if(req.method==='OPTIONS')return res.status(204).end();if(req.method!=='POST')return send(req,res,405,{error:'method_not_allowed'});try{const scope=await installationScope(req);if(scope.toolId!=='vinsansi_whatsapp_manager')throw new Error('tool_scope_mismatch');if(scope.installation.registration_status!=='registered')throw new Error('installation_disabled');const enabled=await scope.client.from('organization_tools').select('enabled').eq('organizations_id',scope.organizationId).eq('tool_id',scope.toolId).maybeSingle();if(enabled.error||enabled.data?.enabled!==true)throw new Error('executor_tool_not_enabled');const input=body(req);const meaningful=input.operation==='rpc'? !['executor_effective_operational_settings','service_worker_heartbeat'].includes(text(input.name)) : ['insert','update','upsert','delete'].includes(text(input.operation));if(meaningful){const touch=await scope.client.rpc('service_touch_tool_installation',{p_organizations_id:scope.organizationId,p_tool_id:scope.toolId,p_external_installation_id:scope.externalInstallationId,p_seen:true,p_meaningful_activity:true,p_installed_version:null,p_reported_capabilities:null,p_last_seen_member_id:null});if(touch.error)throw new Error(touch.error.message);}if(input.operation==='rpc'){const name=text(input.name);if(name==='executor_effective_operational_settings'){const config=await Promise.all([scope.client.from('platform_tools').select('default_settings').eq('tool_id',scope.toolId).single(),scope.client.from('organization_tool_settings').select('settings').eq('organizations_id',scope.organizationId).eq('tool_id',scope.toolId).maybeSingle()]);const settings=config[1].data?.settings??config[0].data?.default_settings??{};return send(req,res,200,{ok:true,data:{dispatch:{whatsapp:settings.whatsapp,chipLevels:settings.chipLevels},timezone:settings.operationalTimezone||'America/Sao_Paulo',cutoffHour:Number(settings.operationalCutoffHour??22)}});}if(!RPCS.has(name))throw new Error('runtime_rpc_forbidden');const rawArgs=(input.args&&typeof input.args==='object')?input.args as Row:{};const args:{[key:string]:unknown}={...rawArgs};const stage11TenantRpc=['service_worker_heartbeat','service_claim_recovery_request','service_complete_recovery_request','refresh_operational_alerts','worker_recover_stale_whatsapp_v2','instagram_recover_stale_items_v2','service_orchestrate_ready_leads'].includes(name);const legacyOrganizationRpc=!stage11TenantRpc&&name!=='service_stage5_converge_automatic_message'&&(Object.prototype.hasOwnProperty.call(args,'p_organizations_id')||name==='service_get_evolution_instances');if(stage11TenantRpc){args.p_organizations_id=scope.organizationId;}else if(legacyOrganizationRpc){const organization=await scope.client.from('organizations').select('legacy_scope_users_id').eq('organizations_id',scope.organizationId).single();if(organization.error)throw new Error(organization.error.message);args.p_users_id=Number(organization.data.legacy_scope_users_id);delete args.p_organizations_id;}else if(name==='service_stage5_converge_automatic_message'){args.p_organizations_id=scope.organizationId;}const result=await scope.client.rpc(name,args);if(result.error)throw new Error(result.error.message);return send(req,res,200,{ok:true,data:result.data});}const table=text(input.table);if(!TABLES.has(table))throw new Error('runtime_table_forbidden');const operation=text(input.operation)||'select';if(!['select','insert','update','upsert','delete'].includes(operation))throw new Error('runtime_operation_forbidden');const global=GLOBAL_TABLES.has(table);let query:any=scope.client.from(table);const rawValues=input.values;const pin=(value:unknown)=>{if(Array.isArray(value))return value.map((row)=>({...row as Row,...(global?{}:{organizations_id:scope.organizationId})}));return {...(value as Row),...(global?{}:{organizations_id:scope.organizationId})};};if(operation==='select')query=query.select(text(input.select)||'*');if(operation==='insert')query=query.insert(pin(rawValues));if(operation==='upsert')query=query.upsert(pin(rawValues),input.options as any);if(operation==='update')query=query.update(pin(rawValues));if(operation==='delete')query=query.delete();query=applyFilters(query,input.filters,scope.organizationId,global);for(const modifier of Array.isArray(input.modifiers)?input.modifiers:[]){const m=modifier as Row;if(m.type==='order')query=query.order(text(m.column),m.options as any);if(m.type==='limit')query=query.limit(Number(m.value));}if(input.returning)query=query.select(text(input.select)||'*');if(input.resultMode==='single')query=query.single();if(input.resultMode==='maybeSingle')query=query.maybeSingle();const result=await query;if(result.error)throw new Error(result.error.message);return send(req,res,200,{ok:true,data:result.data});}catch(error){return send(req,res,executorStatus(error),{ok:false,error:error instanceof Error?error.message:String(error)});}}



